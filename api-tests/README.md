# API Tests — TaskFlow

Автотесты REST API [mini-api](../mini-api/) на Playwright Request API.

## Запуск

```bash
# Терминал 1
cd ../mini-api && npm start

# Терминал 2
npm install
npm test
```

Переменная `API_URL` (по умолчанию `http://localhost:3001`).
